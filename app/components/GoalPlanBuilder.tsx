// Re-export from renamed PlanMode — this shim exists until page.tsx is updated at finalize time
export { default } from "./PlanMode";
export type { PlanModeProps as GoalPlanBuilderProps, PlanStep } from "./PlanMode";
